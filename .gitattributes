# Treat Windows shortcuts as binary (never normalize/diff/merge as text)
*.lnk binary

# Manifests and docs are text; keep LF in the repo
*.csv  text eol=lf
*.md   text eol=lf
*.cff  text eol=lf
LICENSE text eol=lf
